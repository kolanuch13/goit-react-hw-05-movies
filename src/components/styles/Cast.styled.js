import styled from "styled-components";

export const CastStyle = styled.div`
    display: flex;
    align-items: flex-start;
    flex-direction: row;
    flex-wrap: wrap;
    margin: 0px;
    padding: 0px;
    list-style: none;
    ul {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        max-width: 50vw;
    }
`
